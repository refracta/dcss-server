

menu["dcssca"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/forks/crawl-dcssca.txt"
  commands["o"] = exec "ee" "%rrcfiles/crawl-dcssca/%n.rc"
  commands["O"] = exec "virus" "%rrcfiles/crawl-dcssca/%n.rc"
  commands["m"] = exec "ee" "%rrcfiles/crawl-dcssca/%n.macro"
  commands["M"] = exec "virus" "%rrcfiles/crawl-dcssca/%n.macro"
  commands["v"] = exec "ee" "%%CHROOT_CRAWL_BASEDIR%%/crawl-dcssca/data/docs/changelog.txt"
  commands["V"] = exec "virus" "%%CHROOT_CRAWL_BASEDIR%%/crawl-dcssca/data/docs/changelog.txt"
  commands["pP"] = play_game "dcssca"
  commands["A"] = submenu "dcssca-adv"
  commands["wW"] = watch_menu
  commands["qQ"] = return
}

menu["hellcrawl"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/forks/crawl-hellcrawl.txt"
  commands["o"] = exec "ee" "%rrcfiles/crawl-hellcrawl/%n.rc"
  commands["O"] = exec "virus" "%rrcfiles/crawl-hellcrawl/%n.rc"
  commands["m"] = exec "ee" "%rrcfiles/crawl-hellcrawl/%n.macro"
  commands["M"] = exec "virus" "%rrcfiles/crawl-hellcrawl/%n.macro"
  commands["v"] = exec "ee" "%%CHROOT_CRAWL_BASEDIR%%/crawl-hellcrawl/data/docs/changelog.txt"
  commands["V"] = exec "virus" "%%CHROOT_CRAWL_BASEDIR%%/crawl-hellcrawl/data/docs/changelog.txt"
  commands["pP"] = play_game "hellcrawl"
  commands["A"] = submenu "hellcrawl-adv"
  commands["wW"] = watch_menu
  commands["qQ"] = return
}
menu["gnollcrawl"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/forks/crawl-gnollcrawl.txt"
  commands["o"] = exec "ee" "%rrcfiles/crawl-gnollcrawl/%n.rc"
  commands["O"] = exec "virus" "%rrcfiles/crawl-gnollcrawl/%n.rc"
  commands["m"] = exec "ee" "%rrcfiles/crawl-gnollcrawl/%n.macro"
  commands["M"] = exec "virus" "%rrcfiles/crawl-gnollcrawl/%n.macro"
  commands["v"] = exec "ee" "%%CHROOT_CRAWL_BASEDIR%%/crawl-gnollcrawl/data/docs/changelog.txt"
  commands["V"] = exec "virus" "%%CHROOT_CRAWL_BASEDIR%%/crawl-gnollcrawl/data/docs/changelog.txt"
  commands["pP"] = play_game "gnollcrawl"
  commands["A"] = submenu "gnollcrawl-adv"
  commands["wW"] = watch_menu
  commands["qQ"] = return
}
menu["bloatcrawl2"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/forks/crawl-bloatcrawl2.txt"
  commands["o"] = exec "ee" "%rrcfiles/crawl-bloatcrawl2/%n.rc"
  commands["O"] = exec "virus" "%rrcfiles/crawl-bloatcrawl2/%n.rc"
  commands["m"] = exec "ee" "%rrcfiles/crawl-bloatcrawl2/%n.macro"
  commands["M"] = exec "virus" "%rrcfiles/crawl-bloatcrawl2/%n.macro"
  commands["v"] = exec "ee" "%%CHROOT_CRAWL_BASEDIR%%/crawl-bloatcrawl2/data/docs/changelog.txt"
  commands["V"] = exec "virus" "%%CHROOT_CRAWL_BASEDIR%%/crawl-bloatcrawl2/data/docs/changelog.txt"
  commands["pP"] = play_game "bloatcrawl2"
  commands["A"] = submenu "bloatcrawl2-adv"
  commands["wW"] = watch_menu
  commands["qQ"] = return
}
menu["gooncrawl"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/forks/crawl-gooncrawl.txt"
  commands["o"] = exec "ee" "%rrcfiles/crawl-gooncrawl/%n.rc"
  commands["O"] = exec "virus" "%rrcfiles/crawl-gooncrawl/%n.rc"
  commands["m"] = exec "ee" "%rrcfiles/crawl-gooncrawl/%n.macro"
  commands["M"] = exec "virus" "%rrcfiles/crawl-gooncrawl/%n.macro"
  commands["v"] = exec "ee" "%%CHROOT_CRAWL_BASEDIR%%/crawl-gooncrawl/data/docs/changelog.txt"
  commands["V"] = exec "virus" "%%CHROOT_CRAWL_BASEDIR%%/crawl-gooncrawl/data/docs/changelog.txt"
  commands["pP"] = play_game "gooncrawl"
  commands["A"] = submenu "gooncrawl-adv"
  commands["wW"] = watch_menu
  commands["qQ"] = return
}
menu["xcrawl"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/forks/crawl-xcrawl.txt"
  commands["o"] = exec "ee" "%rrcfiles/crawl-xcrawl/%n.rc"
  commands["O"] = exec "virus" "%rrcfiles/crawl-xcrawl/%n.rc"
  commands["m"] = exec "ee" "%rrcfiles/crawl-xcrawl/%n.macro"
  commands["M"] = exec "virus" "%rrcfiles/crawl-xcrawl/%n.macro"
  commands["v"] = exec "ee" "%%CHROOT_CRAWL_BASEDIR%%/crawl-xcrawl/data/docs/changelog.txt"
  commands["V"] = exec "virus" "%%CHROOT_CRAWL_BASEDIR%%/crawl-xcrawl/data/docs/changelog.txt"
  commands["pP"] = play_game "xcrawl"
  commands["A"] = submenu "xcrawl-adv"
  commands["wW"] = watch_menu
  commands["qQ"] = return
}
menu["stoatsoup"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/forks/crawl-stoatsoup.txt"
  commands["o"] = exec "ee" "%rrcfiles/crawl-stoatsoup/%n.rc"
  commands["O"] = exec "virus" "%rrcfiles/crawl-stoatsoup/%n.rc"
  commands["m"] = exec "ee" "%rrcfiles/crawl-stoatsoup/%n.macro"
  commands["M"] = exec "virus" "%rrcfiles/crawl-stoatsoup/%n.macro"
  commands["v"] = exec "ee" "%%CHROOT_CRAWL_BASEDIR%%/crawl-stoatsoup/data/docs/changelog.txt"
  commands["V"] = exec "virus" "%%CHROOT_CRAWL_BASEDIR%%/crawl-stoatsoup/data/docs/changelog.txt"
  commands["pP"] = play_game "stoatsoup"
  commands["A"] = submenu "stoatsoup-adv"
  commands["wW"] = watch_menu
  commands["qQ"] = return
}
menu["kimchicrawl"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/forks/crawl-kimchicrawl.txt"
  commands["o"] = exec "ee" "%rrcfiles/crawl-kimchicrawl/%n.rc"
  commands["O"] = exec "virus" "%rrcfiles/crawl-kimchicrawl/%n.rc"
  commands["m"] = exec "ee" "%rrcfiles/crawl-kimchicrawl/%n.macro"
  commands["M"] = exec "virus" "%rrcfiles/crawl-kimchicrawl/%n.macro"
  commands["v"] = exec "ee" "%%CHROOT_CRAWL_BASEDIR%%/crawl-kimchicrawl/data/docs/changelog.txt"
  commands["V"] = exec "virus" "%%CHROOT_CRAWL_BASEDIR%%/crawl-kimchicrawl/data/docs/changelog.txt"
  commands["pP"] = play_game "kimchicrawl"
  commands["A"] = submenu "kimchicrawl-adv"
  commands["wW"] = watch_menu
  commands["qQ"] = return
}
menu["bcadrencrawl"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/forks/crawl-bcadrencrawl.txt"
  commands["o"] = exec "ee" "%rrcfiles/crawl-bcadrencrawl/%n.rc"
  commands["O"] = exec "virus" "%rrcfiles/crawl-bcadrencrawl/%n.rc"
  commands["m"] = exec "ee" "%rrcfiles/crawl-bcadrencrawl/%n.macro"
  commands["M"] = exec "virus" "%rrcfiles/crawl-bcadrencrawl/%n.macro"
  commands["v"] = exec "ee" "%%CHROOT_CRAWL_BASEDIR%%/crawl-bcadrencrawl/data/docs/changelog.txt"
  commands["V"] = exec "virus" "%%CHROOT_CRAWL_BASEDIR%%/crawl-bcadrencrawl/data/docs/changelog.txt"
  commands["pP"] = play_game "bcadrencrawl"
  commands["A"] = submenu "bcadrencrawl-adv"
  commands["wW"] = watch_menu
  commands["qQ"] = return
}



menu["crawl-0.31"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/games/crawl-0.31.txt"
  commands["o"] = exec "ee" "%rrcfiles/crawl-0.31/%n.rc"
  commands["O"] = exec "virus" "%rrcfiles/crawl-0.31/%n.rc"
  commands["m"] = exec "ee" "%rrcfiles/crawl-0.31/%n.macro"
  commands["M"] = exec "virus" "%rrcfiles/crawl-0.31/%n.macro"
  commands["v"] = exec "ee" "%%CHROOT_CRAWL_GAMEDIR%%/data/docs/crawl_changelog.txt"
  commands["V"] = exec "virus" "%%CHROOT_CRAWL_GAMEDIR%%/data/docs/crawl_changelog.txt"
  commands["pP"] = play_game "dcss-0.31"
  commands["dD"] = play_game "dcss-0.31-descent"
  commands["tT"] = play_game "dcss-0.31-tutorial"
  commands["sS"] = play_game "dcss-0.31-sprint"
  commands["A"] = submenu "crawl-0.31-adv"
  commands["wW"] = watch_menu
  commands["qQ"] = return
}

menu["dcssca-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/forks/crawl-dcssca_adv.txt"
  commands["B"] =
    exec "/bin/savegame-backup.sh" "%n crawl-dcssca %%CHROOT_CRAWL_BASEDIR%%",
    return
  commands["O"] =
    cp "%rdata/crawl-dcssca-settings/init.txt" "%rrcfiles/crawl-dcssca/%n.rc",
    return
  commands["M"] =
    cp "%rdata/crawl-git.macro" "%rrcfiles/crawl-dcssca/%n.macro",
    return
  commands["qQ"] = return
}

menu["hellcrawl-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/forks/crawl-hellcrawl.txt"
  commands["B"] =
    exec "/bin/savegame-backup.sh" "%n crawl-hellcrawl %%CHROOT_CRAWL_BASEDIR%%",
    return
  commands["O"] =
    cp "%rdata/crawl-hellcrawl-settings/init.txt" "%rrcfiles/crawl-hellcrawl/%n.rc",
    return
  commands["M"] =
    cp "%rdata/crawl-git.macro" "%rrcfiles/crawl-hellcrawl/%n.macro",
    return
  commands["qQ"] = return
}

menu["gnollcrawl-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/forks/crawl-gnollcrawl.txt"
  commands["B"] =
    exec "/bin/savegame-backup.sh" "%n crawl-gnollcrawl %%CHROOT_CRAWL_BASEDIR%%",
    return
  commands["O"] =
    cp "%rdata/crawl-gnollcrawl-settings/init.txt" "%rrcfiles/crawl-gnollcrawl/%n.rc",
    return
  commands["M"] =
    cp "%rdata/crawl-git.macro" "%rrcfiles/crawl-gnollcrawl/%n.macro",
    return
  commands["qQ"] = return
}

menu["bloatcrawl2-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/forks/crawl-bloatcrawl2.txt"
  commands["B"] =
    exec "/bin/savegame-backup.sh" "%n crawl-bloatcrawl2 %%CHROOT_CRAWL_BASEDIR%%",
    return
  commands["O"] =
    cp "%rdata/crawl-bloatcrawl2-settings/init.txt" "%rrcfiles/crawl-bloatcrawl2/%n.rc",
    return
  commands["M"] =
    cp "%rdata/crawl-git.macro" "%rrcfiles/crawl-bloatcrawl2/%n.macro",
    return
  commands["qQ"] = return
}

menu["gooncrawl-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/forks/crawl-gooncrawl.txt"
  commands["B"] =
    exec "/bin/savegame-backup.sh" "%n crawl-gooncrawl %%CHROOT_CRAWL_BASEDIR%%",
    return
  commands["O"] =
    cp "%rdata/crawl-gooncrawl-settings/init.txt" "%rrcfiles/crawl-gooncrawl/%n.rc",
    return
  commands["M"] =
    cp "%rdata/crawl-git.macro" "%rrcfiles/crawl-gooncrawl/%n.macro",
    return
  commands["qQ"] = return
}

menu["xcrawl-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/forks/crawl-xcrawl.txt"
  commands["B"] =
    exec "/bin/savegame-backup.sh" "%n crawl-xcrawl %%CHROOT_CRAWL_BASEDIR%%",
    return
  commands["O"] =
    cp "%rdata/crawl-xcrawl-settings/init.txt" "%rrcfiles/crawl-xcrawl/%n.rc",
    return
  commands["M"] =
    cp "%rdata/crawl-git.macro" "%rrcfiles/crawl-xcrawl/%n.macro",
    return
  commands["qQ"] = return
}

menu["stoatsoup-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/forks/crawl-stoatsoup.txt"
  commands["B"] =
    exec "/bin/savegame-backup.sh" "%n crawl-stoatsoup %%CHROOT_CRAWL_BASEDIR%%",
    return
  commands["O"] =
    cp "%rdata/crawl-stoatsoup-settings/init.txt" "%rrcfiles/crawl-stoatsoup/%n.rc",
    return
  commands["M"] =
    cp "%rdata/crawl-git.macro" "%rrcfiles/crawl-stoatsoup/%n.macro",
    return
  commands["qQ"] = return
}

menu["kimchicrawl-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/forks/crawl-kimchicrawl.txt"
  commands["B"] =
    exec "/bin/savegame-backup.sh" "%n crawl-kimchicrawl %%CHROOT_CRAWL_BASEDIR%%",
    return
  commands["O"] =
    cp "%rdata/crawl-kimchicrawl-settings/init.txt" "%rrcfiles/crawl-kimchicrawl/%n.rc",
    return
  commands["M"] =
    cp "%rdata/crawl-git.macro" "%rrcfiles/crawl-kimchicrawl/%n.macro",
    return
  commands["qQ"] = return
}

menu["bcadrencrawl-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/forks/crawl-bcadrencrawl.txt"
  commands["B"] =
    exec "/bin/savegame-backup.sh" "%n crawl-bcadrencrawl %%CHROOT_CRAWL_BASEDIR%%",
    return
  commands["O"] =
    cp "%rdata/crawl-bcadrencrawl-settings/init.txt" "%rrcfiles/crawl-bcadrencrawl/%n.rc",
    return
  commands["M"] =
    cp "%rdata/crawl-git.macro" "%rrcfiles/crawl-bcadrencrawl/%n.macro",
    return
  commands["qQ"] = return
}

menu["crawl-git-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/games/crawl-git_adv.txt"
  commands["B"] =
    exec "/bin/savegame-backup.sh" "%n crawl-git %%CHROOT_CRAWL_BASEDIR%%",
    return
  commands["O"] =
    cp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    return
  commands["M"] =
    cp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    return
  commands["qQ"] = return
}

menu["crawl-0.31-adv"] {
  bannerfile = "%%CHROOT_DGLDIR%%/data/menus/games/crawl-0.31_adv.txt"
  commands["B"] =
    exec "/bin/savegame-backup.sh" "%n crawl-0.31 %%CHROOT_CRAWL_BASEDIR%%",
    return
  commands["O"] =
    cp "%rdata/crawl-0.31-settings/init.txt" "%rrcfiles/crawl-0.31/%n.rc",
    return
  commands["M"] =
    cp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.31/%n.macro",
    return
  commands["qQ"] = return
}

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-git/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"
##############################################################################
##  DCSSCA
##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "DCSS Circus Animals"
  short_name = "dcssca"

  game_args = "/bin/crawl-stable-launcher.sh", "dcssca",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-dcssca/%n.rc",
              "-macro", "%rrcfiles/crawl-dcssca/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcssca.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-dcssca/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-dcssca/%n.macro",
    ifnxcp "%rdata/crawl-dcssca-settings/init.txt" "%rrcfiles/crawl-dcssca/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
##  HELLCRAWL
##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "HellCrawl"
  short_name = "hellcrawl"

  game_args = "/bin/crawl-stable-launcher.sh", "hellcrawl",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-hellcrawl/%n.rc",
              "-macro", "%rrcfiles/crawl-hellcrawl/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:hellcrawl.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-hellcrawl/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-hellcrawl/%n.macro",
    ifnxcp "%rdata/crawl-hellcrawl-settings/init.txt" "%rrcfiles/crawl-hellcrawl/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
##  GNOLLCRAWL
##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "GnollCrawl"
  short_name = "gnollcrawl"

  game_args = "/bin/crawl-stable-launcher.sh", "gnollcrawl",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-gnollcrawl/%n.rc",
              "-macro", "%rrcfiles/crawl-gnollcrawl/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:gnollcrawl.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-gnollcrawl/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-gnollcrawl/%n.macro",
    ifnxcp "%rdata/crawl-gnollcrawl-settings/init.txt" "%rrcfiles/crawl-gnollcrawl/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
##  BLOATCRAWL 2
##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "BloatCrawl 2"
  short_name = "bloatcrawl2"

  game_args = "/bin/crawl-stable-launcher.sh", "bloatcrawl2",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-bloatcrawl2/%n.rc",
              "-macro", "%rrcfiles/crawl-bloatcrawl2/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:bloatcrawl2.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-bloatcrawl2/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-bloatcrawl2/%n.macro",
    ifnxcp "%rdata/crawl-bloatcrawl2-settings/init.txt" "%rrcfiles/crawl-bloatcrawl2/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
##  GOONCRAWL
##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "GoonCrawl"
  short_name = "gooncrawl"

  game_args = "/bin/crawl-stable-launcher.sh", "gooncrawl",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-gooncrawl/%n.rc",
              "-macro", "%rrcfiles/crawl-gooncrawl/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:gooncrawl.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-gooncrawl/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-gooncrawl/%n.macro",
    ifnxcp "%rdata/crawl-gooncrawl-settings/init.txt" "%rrcfiles/crawl-gooncrawl/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
##  XCRAWL
##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "X-Crawl"
  short_name = "xcrawl"

  game_args = "/bin/crawl-stable-launcher.sh", "xcrawl",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-xcrawl/%n.rc",
              "-macro", "%rrcfiles/crawl-xcrawl/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:xcrawl.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-xcrawl/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-xcrawl/%n.macro",
    ifnxcp "%rdata/crawl-xcrawl-settings/init.txt" "%rrcfiles/crawl-xcrawl/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
##  Stoat Soup
##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Stoat Soup"
  short_name = "stoatsoup"

  game_args = "/bin/crawl-stable-launcher.sh", "stoatsoup",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-stoatsoup/%n.rc",
              "-macro", "%rrcfiles/crawl-stoatsoup/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:stoatsoup.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-stoatsoup/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-stoatsoup/%n.macro",
    ifnxcp "%rdata/crawl-stoatsoup-settings/init.txt" "%rrcfiles/crawl-stoatsoup/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
##  KIMCHICRAWL
##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "KimchiCrawl"
  short_name = "kimchicrawl"

  game_args = "/bin/crawl-stable-launcher.sh", "kimchicrawl",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-kimchicrawl/%n.rc",
              "-macro", "%rrcfiles/crawl-kimchicrawl/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:kimchicrawl.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-kimchicrawl/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-kimchicrawl/%n.macro",
    ifnxcp "%rdata/crawl-kimchicrawl-settings/init.txt" "%rrcfiles/crawl-kimchicrawl/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
##  BCADRENCRAWL
##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "BcadrenCrawl"
  short_name = "bcadrencrawl"

  game_args = "/bin/crawl-stable-launcher.sh", "bcadrencrawl",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-bcadrencrawl/%n.rc",
              "-macro", "%rrcfiles/crawl-bcadrencrawl/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:bcadrencrawl.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-bcadrencrawl/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-bcadrencrawl/%n.macro",
    ifnxcp "%rdata/crawl-bcadrencrawl-settings/init.txt" "%rrcfiles/crawl-bcadrencrawl/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
##  GIT
##############################################################################
DEFINE {
  game_path = "/bin/crawl-git-launcher.sh"
  game_name = "Crawl (trunk)"
  short_name = "dcss-git"

  game_args = "/bin/crawl-git-launcher.sh",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-git.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600


  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-git-launcher.sh"
  game_name = "Sprint (trunk)"
  short_name = "dcss-git-sprint"

  game_args = "/bin/crawl-git-launcher.sh",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-git-sprint.sock",
              "-morgue", "%rmorgue/%n/",
              "-sprint"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-git-sprint/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-git-launcher.sh"
  game_name = "Tutorial (trunk)"
  short_name = "dcss-git-tutorial"

  game_args = "/bin/crawl-git-launcher.sh",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-git-tutorial.sock",
              "-morgue", "%rmorgue/%n/",
              "-tutorial"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-git-tutorial/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-git-launcher.sh"
  game_name = "Descent (trunk)"
  short_name = "dcss-git-descent"

  game_args = "/bin/crawl-git-launcher.sh",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-git/%n.rc",
              "-macro", "%rrcfiles/crawl-git/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-git-descent.sock",
              "-morgue", "%rmorgue/%n/",
              "-descent"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-git-descent/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-git/%n.macro",
    ifnxcp "%rdata/crawl-git-settings/init.txt" "%rrcfiles/crawl-git/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
##  0.31
##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Crawl (0.31)"
  short_name = "dcss-0.31"

  game_args = "/bin/crawl-stable-launcher.sh", "0.31",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.31/%n.rc",
              "-macro", "%rrcfiles/crawl-0.31/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-0.31.sock",
              "-morgue", "%rmorgue/%n/"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-0.31/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-git.macro" "%rrcfiles/crawl-0.31/%n.macro",
    ifnxcp "%rdata/crawl-0.31-settings/init.txt" "%rrcfiles/crawl-0.31/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Sprint (0.31)"
  short_name = "dcss-0.31-sprint"

  game_args = "/bin/crawl-stable-launcher.sh",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.31/%n.rc",
              "-macro", "%rrcfiles/crawl-0.31/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-0.31-sprint.sock",
              "-morgue", "%rmorgue/%n/",
              "-sprint"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-0.31-sprint/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-0.31.macro" "%rrcfiles/crawl-0.31/%n.macro",
    ifnxcp "%rdata/crawl-0.31-settings/init.txt" "%rrcfiles/crawl-0.31/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Tutorial (0.31)"
  short_name = "dcss-0.31-tutorial"

  game_args = "/bin/crawl-stable-launcher.sh",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.31/%n.rc",
              "-macro", "%rrcfiles/crawl-0.31/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-0.31-tutorial.sock",
              "-morgue", "%rmorgue/%n/",
              "-tutorial"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-0.31-tutorial/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-0.31.macro" "%rrcfiles/crawl-0.31/%n.macro",
    ifnxcp "%rdata/crawl-0.31-settings/init.txt" "%rrcfiles/crawl-0.31/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}

##############################################################################
DEFINE {
  game_path = "/bin/crawl-stable-launcher.sh"
  game_name = "Descent (0.31)"
  short_name = "dcss-0.31-descent"

  game_args = "/bin/crawl-stable-launcher.sh",
              "-name", "%n",
              "-rc", "%rrcfiles/crawl-0.31/%n.rc",
              "-macro", "%rrcfiles/crawl-0.31/%n.macro",
              "-webtiles-socket", "%%CHROOT_CRAWL_BASEDIR%%/webserver/sockets/%n:dcss-0.31-descent.sock",
              "-morgue", "%rmorgue/%n/",
              "-descent"

  extra_info_file = "%rmorgue/%n/%n.dglwhere"

  #
  # From inside the jail, where dgamelaunch should put mail - should
  # match up with the game's settings.
  spooldir = "%%CHROOT_MAILSPOOL%%/"

  max_idle_time = 3600

  # make sure the inprogress dir actually exists. default is "inprogress/"
  # each game you define here must have it's own.
  inprogressdir = "%rinprogress/crawl-0.31-descent/"
  ttyrecdir = "%%CHROOT_TTYRECDIR%%/%n/"

  commands =
    ifnxcp "%rdata/crawl-0.31.macro" "%rrcfiles/crawl-0.31/%n.macro",
    ifnxcp "%rdata/crawl-0.31-settings/init.txt" "%rrcfiles/crawl-0.31/%n.rc",
    setenv "MAIL" "%%CHROOT_MAILSPOOL%%/%n",
    setenv "SIMPLEMAIL" "1",
    unlink "%%CHROOT_MAILSPOOL%%/%n",
    mkdir "%rmorgue/%n/"

  encoding = "UTF-8"
}
